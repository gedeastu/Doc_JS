// "FOR" LOOPING
for(let health = 5; health >= 0; health--){
    console.log(health);
}

// COMBINATION
// for & array
let footballClub = [`MU`,`MC`,`RM`,`LV`];
for(let i = 0; i < footballClub.length;i++){
    console.log(footballClub[i]);
}
